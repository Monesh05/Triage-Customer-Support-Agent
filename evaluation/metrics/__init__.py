# evaluation/metrics/__init__.py
# Purpose: Marks `evaluation.metrics` as a package. Public entry points are
#          `evaluation.metrics.aggregate.compute_full_report` (used by run_eval.py) and the
#          individual per-metric modules (routing.py, tool_calls.py, resolution.py, qa.py,
#          escalation.py, latency.py), each independently unit-testable — see module docstrings.
# Author: CloudDesk Team
# Date: 2026-09-24
